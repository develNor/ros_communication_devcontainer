# Source-checkout rosotacom example setup.
#
# Relative paths are resolved from this file's directory.
# This file is not discovered automatically; use:
#   eval "$(rosotacom setup-env ./rosotacom.yaml)"
ros2docker_config: examples/ros2docker.json
session_configs_dir: examples/sessions
data_dict: examples/data_dict.json
