# ignore local system specific settings
/.devcontainer/devcontainer.json
/ros2docker.json
/session-instances/
/zenoh.json5

# ignore temporary data
__pycache__
.coverage
.mypy_cache/
.pytest_cache/
.ruff_cache/
.venv/
/artifacts/
/build/
/dist/
coverage.xml
*.egg-info/
.session_readonly.yaml
.codex
examples/sessions/**/a/**
examples/sessions/**/b/**
examples/sessions/**/a_to_b_topics.txt
examples/sessions/**/b_to_a_topics.txt
examples/sessions/**/qos.yaml
src/rosotacom/resources/examples/sessions/**/a/**
src/rosotacom/resources/examples/sessions/**/b/**
src/rosotacom/resources/examples/sessions/**/a_to_b_topics.txt
src/rosotacom/resources/examples/sessions/**/b_to_a_topics.txt
src/rosotacom/resources/examples/sessions/**/qos.yaml
src/rosotacom/resources/examples/session-instances/
CLAUDE.md

# The dedicated agent account's credential lives here and must never reach a
# commit. See docs/github-repository-settings.md, "Identities".
#
# No trailing slash, deliberately: `.agents/` matches a directory only, so a
# `.agents` *symlink* — the shape a worktree gets when it points at the main
# checkout's credential — slipped past it and was committed (#238). Without the
# slash the pattern covers file, directory and symlink alike, and
# tests/contract/test_packaged_resources.py fails if one is ever tracked again.
.agents
time-bins.jsonl
