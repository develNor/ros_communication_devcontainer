parameters:
  session_dir: /ws/example/5_sized_payload
  peer_dir: ${session_dir}/b

  ip_local: tks-leitstand-02_tks
  local_name: b
  ip_remote: tks-seat_tks
  remote_name: a

  qos_config_file: ${session_dir}/qos.yaml

  in: true
  topic_list_paths_inbound: ${session_dir}/a_to_b_topics.txt

  out: true
  topic_list_paths_outbound: ${session_dir}/b_to_a_topics.txt

  topic_monitor: true

  otaw: true
  otaw_config_file: ${peer_dir}/ota_wrapper.yaml
  otaw_suffix: /ota_stamped

  otau: true
  otau_config_file: ${peer_dir}/ota_unwrapper.yaml
  otau_suffix: /ota_stamped
