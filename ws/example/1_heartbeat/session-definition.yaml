peers:
  a:
    ip_key: "machine_a_ip"
  b:
    ip_key: "machine_b_ip"

peer_settings:
  a:
    domain_id: 46
  b:
    domain_id: 47

shared:
  use_heartbeat: true
  ota_domain_id: 48

  # Tested Working RMW-Config Combinations:

  # 1. FastDDS (works only sometimes weirdly)
  rmw: fastdds

# works sometimes?
# gleiche config (peerlist = locale + remote ). Geht in 2/4 fällen
#   keine first message bei 
# - Seat IN-Relay, OUT-Bridge und bei  (keine com zwischen local und ota?) (URSACHE)
# - Leitstand IN-Relaay + Bridge (weil schon nichts ankommt von SEAT)
# - problem ist die domain bridge, die nicht geht plötzlich
# pures fastdds ging auch manchmal


  # 2. Cyclone with OTA config (working)
  # rmw:
  #   local: cyclone
  #   ota:
  #     cyclone:
  #       config: cyclonedds_minimal.xml

  # 3. Zenoh with connected endpoints (working)
  # rmw: 
  #   local: zenoh
  #   ota: zenoh_connect_endpoints

  # 4. Cyclone with OTA config and locally FastDDS (working)
  # rmw:
  #   local: fastdds
  #   ota:
  #     cyclone:
  #       config: cyclonedds_minimal.xml

  # 5. FastDDS for OTA and locally Cyclone
  # rmw:
  #   local: cyclone
  #   ota: fastdds

  # 6. Zenoh-ROS2DDS for OTA and locally cyclone (working)
  # rmw:
  #   local: cyclone
  #   ota: zenoh_ros2dds

  # 7. Zenoh-ROS2DDS for OTA and locally fastdds (not working weird message dublication)
  # rmw:
  #   local: fastdds
  #   ota: zenoh_ros2dds

